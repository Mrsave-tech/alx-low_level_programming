#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always
 *Author: Mrsave
 */

int main(void)
{
	char alpha;
	for (alpha = 'a'; alpha <= 'z'; alpha++)
		putchar(alpha);
	putchar('\n');
	return (0);
}
