#include "holberton.h"

/**
 * print_alphabet - void print_alphabet_x10, followed by a new line.
 */
void print_alphabet_x10(void);
{
	char l;
	int x;
	for (x = 0; x <10; x++)
	  {
	for (l = 'a'; l <= 'z'; l++)
		_putchar(l);
	  }
	_putchar('\n');
}
